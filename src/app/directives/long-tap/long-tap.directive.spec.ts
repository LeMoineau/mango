import { LongTapDirective } from './long-tap.directive';

describe('LongTapDirective', () => {
  it('should create an instance', () => {
    const directive = new LongTapDirective();
    expect(directive).toBeTruthy();
  });
});
